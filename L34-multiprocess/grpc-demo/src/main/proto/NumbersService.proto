syntax = "proto3";

package ru.otus.protobuf;

option java_multiple_files = true;

message NumbersRange {
    int32 firstValue = 1;
    int32 lastValue = 2;
}

message NumberMessage {
    int32 value = 1;
}

service NumbersService {
    rpc generate(NumbersRange) returns (stream NumberMessage);
}
